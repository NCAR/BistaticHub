Installation Instructions for 'BistaticHub' and Utilities
=========================================================

If you don't have a Makefile in this directory, execute the following commands
to generate it:

    $ aclocal
    $ autoconf
    $ automake
    $ ./configure

Once you have a Makefile, generate the BistaticHub, pb_fakesource, and
vb_fakesource programs by executing:

    $ make
